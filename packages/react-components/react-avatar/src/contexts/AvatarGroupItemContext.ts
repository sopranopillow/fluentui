import { createContext } from '@fluentui/react-context-selector';
import type { Context } from '@fluentui/react-context-selector';
import type { AvatarGroupItemContextValue } from './AvatarGroupItemContext.types';

/**
 * TODO
 */
export const AvatarGroupItemContext: Context<AvatarGroupItemContextValue> = createContext({});
